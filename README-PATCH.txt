SOFEA orange-only patch

Copy the contents of this directory into the repository root and overwrite the
existing files. Then run:

  python scripts\build.py
  git add .
  git commit -m "Simplify design and update SoundCloud integration"
  git push

The patch does not overwrite content/episodes.json.
